<h1>投稿一覧ページ</h1>

<div class="container">
	<div class="row">
		<% @posts.each do |post| %>
		<table>
			 <tr>
				<th><%= attachment_image_tag post, :image, format: 'jpeg', size: "600x400" %></th>
				<th><%= post.recipe_name %></th>
				<th><%= post.caption %></th>
			</tr>
		</table>
			<% end %>