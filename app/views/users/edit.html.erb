<h1>プロフィール編集</h1>
<%= form_for(@user) do |f| %>
	<p>画像</p>
	<%= f.attachment_field :profile_image %>
	<p>名前</p>
	<%= f.text_field :name %>
	<p>自己紹介</p>
	<%= f.text_area :userinfo %>

	<%= f.submit "保存" %>
<% end %>